# Image from https://hub.docker.com/_/php/
image: node:latest

cache:
  paths:
  - node_modules/

before_script:
  - npm install hexo-cli -g
  - npm install hexo-deployer-rsync --save
  - npm install

test:base:
  script:
    - hexo generate
    - hexo deploy